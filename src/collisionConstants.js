//Constants used in Collisions masks
const SCORINGAREAS = Math.pow(2, 0);
const BOUNDS = Math.pow(2, 1);
const REDCUE = Math.pow( 2, 2);
const BLUECUE = Math.pow( 2, 3);
const REDDISCS = Math.pow( 2, 4);
const BLUEDISCS = Math.pow( 2, 5);

export {SCORINGAREAS, BOUNDS, REDCUE, BLUECUE, REDDISCS, BLUEDISCS}
